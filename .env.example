# Backend API root (no trailing slash). Include version prefix.
# Leave empty to use same-origin /api/v1 (Vite dev proxy locally, vercel.json rewrite in production).
# Optional for local dev against a remote backend: https://web-production-62118.up.railway.app/api/v1
VITE_API_BASE_URL=

# Set to true when the API uses HttpOnly cookies / credentialed cross-origin requests
VITE_API_WITH_CREDENTIALS=false

# OAuth (optional — used by src/const.ts)
VITE_OAUTH_PORTAL_URL=
VITE_APP_ID=

# Google Maps proxy (optional — used by src/components/Map.tsx)
VITE_FRONTEND_FORGE_API_KEY=
VITE_FRONTEND_FORGE_API_URL=https://forge.butterfly-effect.dev

# Analytics (optional — injected into index.html at build time)
VITE_ANALYTICS_ENDPOINT=
VITE_ANALYTICS_WEBSITE_ID=
